<br />
<br />
<div class="row">
<div class="col-md-6 col-md-offset-3 text-center">
    <h3>Future Home Of the Fall Gambit Chess App!</h3>
    <br />
    <p>
      The time is now: <%= Time.now %>
    </p>
    <% if @games.present? %>
      <% @games.each do |game| %>
        <% if game.white_user.nil? || game.black_user.nil? %>
          <h2>
            <%= game.game_name %>
          </h2>
        <% else %>
          <h2>
            Looks like there's no game available... Create a new one!
          </h2>
        <% end  %>
      <% end %>
    <% else %>
      <h2>
        No games available...
      </h2>
    <% end %>
  </div>
</div>
